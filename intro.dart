main() {
  List<Map> numbers = [
    {'one': 1},
    {'one': 1},
    {'one': 1}
  ];
  print(numbers);
  print(numbers[0]['one']);
  Map numbers2 = {'one': 1, 'two': 2, 'three': 3};
  print(numbers2['two']);
}
